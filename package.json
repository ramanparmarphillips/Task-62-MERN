{
  "name": "task-62-mern",
  "version": "1.0.0",
  "description": "Task 61",
  "main": "server.js",
  "scripts": {
    "install-dependencies": "cd frontend && npm install && cd ../Backend && npm install && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1",
    "startBack": "node Backend/server.js",
    "server": "nodemon Backend/server.js",
    "client": "npm start --prefix frontend",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "start2": "cd frontend && npm install && cd ../Backend && npm install && cd .. && concurrently \"npm run server\" \"npm run client\"",
    
    "start": "npm install && npm install --prefix frontend && npm install --prefix Backend && concurrently \"npm run server\" \"npm run client\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ramanparmarphillips/Task-62-MERN.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ramanparmarphillips/Task-62-MERN/issues"
  },
  "homepage": "https://github.com/ramanparmarphillips/Task-62-MERN#readme",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "colors": "^1.4.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.2"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.21"
  }
}
